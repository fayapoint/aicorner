import { NextRequest, NextResponse } from 'next/server';

export async function POST(request: NextRequest) {
  try {
    const authHeader = request.headers.get('authorization');
    if (!authHeader || !authHeader.startsWith('Bearer ')) {
      return NextResponse.json(
        { error: 'No token provided' },
        { status: 401 }
      );
    }

    // For local development, return a mock video URL
    const mockVideoUrl = 'https://sample-videos.com/zip/10/mp4/SampleVideo_1280x720_1mb.mp4';
    const mockThumbnailUrl = 'data:image/svg+xml;base64,';
    
    console.log('Mock video uploaded for local development');
    
    return NextResponse.json({
      videoUrl: mockVideoUrl,
      thumbnailUrl: mockThumbnailUrl,
      publicId: `mock-video-${Date.now()}`,
      duration: 60,
      width: 1280,
      height: 720,
      format: 'mp4',
      bytes: 1048576
    });
  } catch (error) {
    return NextResponse.json(
      { error: 'Internal server error' },
      { status: 500 }
    );
  }
}

export async function OPTIONS() {
  return new NextResponse(null, {
    status: 200,
    headers: {
      'Access-Control-Allow-Origin': '*',
      'Access-Control-Allow-Methods': 'POST, OPTIONS',
      'Access-Control-Allow-Headers': 'Content-Type, Authorization',
    },
  });
}
